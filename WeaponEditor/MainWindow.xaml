<Window x:Class="WeaponEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filters -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBox x:Name="FilterNameTextBox" Width="200" Margin="0,0,10,0" TextChanged="FilterNameTextChanged"/>

            <ComboBox x:Name="FilterTypeComboBox" Width="150" SelectionChanged="FilterTypeOnlySelectionChanged"/>
        </StackPanel>

        <!-- Weapon List -->
        <ListBox x:Name="WeaponListBox" Grid.Row="2"/>
    </Grid>
</Window>
